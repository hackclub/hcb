<!DOCTYPE html>
<html lang="en" data-dark="<%= @is_dark %>">
  <head>
    <%= render "layouts/head", title_suffix: "HCB Admin", admin: true %>
    <%= stylesheet_link_tag "admin", media: "all", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "admin", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "application", 'data-turbo-track': "reload", defer: true %>
    <%= javascript_include_tag "bundle", 'data-turbo-track': "reload", defer: true %>
  </head>
  <body class="overflow-x-hidden min-h-screen flex flex-col max-w-[100vw]">
    <a class="skip-to-main" href="#main">Skip to main content</a>
    <% title = yield(:title) if content_for?(:title) %>
    <% nav = Admin::Nav.new(page_title: title) %>

    <nav class="flex flex-col shadow-lg sticky top-0 backdrop-blur-lg z-10 bg-white/80 dark:!bg-[rgba(0,0,0,0.8)]">
      <div class="p-6 pl-5 pb-2 flex items-center w-full gap-2">
        <%= render "application/logo" %>

        <span class="text-xl font-bold ml-1 hidden sm:inline-block">HCB Admin</span>
        <span style="color: var(--hcb-link-2); font-weight: 200;">/</span>

        <% if title %>
          <% if nav.active_section&.name %>
          <span class="truncate max-w-[50px] sm:max-w-[100px] md:max-w-[200px] lg:max-w-[300px] xl:max-w-[400px]">
            <%= nav.active_section&.name %>
          </span>
          <span style="color: var(--hcb-link-2); font-weight: 200;">/</span>
          <% end %>
          <span class="truncate max-w-[50px] sm:max-w-[100px] md:max-w-[200px] lg:max-w-[300px] xl:max-w-[400px]">
            <%= title %>
          </span>
        <% else %>
          <b class="truncate max-w-[50px] sm:max-w-[100px] md:max-w-[200px] lg:max-w-[300px] xl:max-w-[400px]">
            <%= current_user.access_level.capitalize %>
          </b>
        <% end %>

        <div class="flex-1 gap-1 flex justify-end items-center">
          <%= link_to flipper_path, 'aria-label': "View transactions awaiting receipts", class: "menu__action w-8 h-8 justify-center tooltipped" do %>
            <%= inline_icon "flag", size: home_action_size %>
          <% end %>
          <%= link_to blazer_path, 'aria-label': "View transactions awaiting receipts", class: "menu__action w-8 h-8 justify-center" do %>
            <%= inline_icon "terminal", size: home_action_size %>
          <% end %>
          <div class="w-0.5 h-7 bg-gray-200 mx-2 rounded-full"></div>

          <div data-controller="menu">
            <a
              href="javascript:void(0)"
              class="menu__action w-8 h-8 justify-center"
              data-action="menu#toggle click@document->menu#close keydown@document->menu#keydown"
              data-menu-target="toggle">
              <%= inline_icon "moon", size: 28 %>
            </a>
            <div data-controller="dark-mode-toggle" class="menu__content menu__content--compact menu__content--2 p-2 -mt-2" data-menu-target="content" style="z-index: 999;">
              <%= render "application/theme_toggle" %>
            </div>
          </div>
          <%= render partial: "application/blog_widget" %>
          <%= link_to my_inbox_path, 'aria-label': "View transactions awaiting receipts", class: "menu__action w-8 h-8 justify-center" do %>
            <%= inline_icon "receipt", size: home_action_size %>
          <% end %>
          <%= link_to logout_users_path, method: :delete, id: "admin-logout-button", 'aria-label': "Sign out", class: "menu__action w-8 h-8 justify-center" do %>
            <%= inline_icon "door-leave", size: home_action_size %>
          <% end %>
        </div>
      </div>

      <%= turbo_frame_tag "admin_nav", src: nav_admin_index_path(title:), class: "w-100" do %>
        <%= render partial: "admin/nav", locals: { nav:, skeleton: true } %>
      <% end %>
    </nav>

    <%= render(partial: "admin/flash") %>

    <div id="main" class="bg-snow dark:bg-dark flex-1 w-[100vw] overflow-x-auto overflow-y-auto" style="padding: 2rem; margin: 0 auto">
      <div class="flex w-full mb-2 items-center gap-2">
        <h2 class="flex-1"><%= title %></h2>
        <%= yield(:admin_actions) %>
      </div>
      <%= yield %>
    </div>

    <%= render "application/command_bar" %>
  </body>
</html>
