<%# locals: (form:, model:, title:, description:, icon:, checked: false) %>

<% unsupported = IllegalEntity::PayoutMethod.unsupported?(model) %>
<%= form.radio_button :payout_method_type, model.name, checked:, disabled: unsupported %>
<%= form.label :payout_method_type, value: model.name do %>
  <%= inline_icon icon, size: 28 %>
  <strong><%= title %></strong>

  <%= yield %><%# in case you wanna add more flare to a specific option %>

  <% if unsupported %>
    <div class="badge bg-warning m1 mt0 w-fit"><%= IllegalEntity::PayoutMethod.unsupported_details(model)[:status_badge] %></div>
    <small><%= IllegalEntity::PayoutMethod.unsupported_details(model)[:reason] %></small>
  <% else %>
    <small><%= description %></small>
  <% end %>
<% end %>
