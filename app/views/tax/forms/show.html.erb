<% title "Tax form for #{@legal_entity.name}" %>

<h1>Tax form for <%= @legal_entity.name %></h1>

<iframe
  src="<%= @form.signing_url %>"
  class="border-0 relative h-[75vh] w-[50vw] rounded-md"
>
</iframe>
