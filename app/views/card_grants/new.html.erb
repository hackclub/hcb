<% page_sm %>
<%= render "events/nav", selected: :transfers %>
<% disabled = !policy(@card_grant).create? %>

<%= modal_header "Send a grant" %>

<%= render "new_form", locals: {card_grant: @card_grant} %>
