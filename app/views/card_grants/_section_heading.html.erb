<%# locals: (icon:, title:) %>

<h2 class="grant-section">
  <% unless organizer_signed_in? %>
    <span class="grant-section__icon" aria-hidden="true"><%= icon %></span>
  <% end %>
  <%= title %>
</h2>
