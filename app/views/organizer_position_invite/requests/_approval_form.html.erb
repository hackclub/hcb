<%# locals: (invite_request:) %>

<%= form_with(local: true, url: approve_request_path(invite_request), data: { controller: "conditional-fields", conditional_fields_state_value: { role: "member" }.to_json }) do |form| %>
  <%= render "organizer_position_invites/role_and_control_form", form: form %>

  <%= form.submit "Submit" %>
<% end %>
